<!DOCTYPE html>
  <html lang="en">
    <body style = "background-color: lightblue">
    <% include partials/_header %>
      <table>
      <tr>
        <td><%= shortURL %></td>
        <td>&rarr;</td>
      <form action="/urls/<%= shortURL %>" method="POST" style="margin:150px;">
        <label for="longURL">Update a URL:</label>
        <input id="longURL" type="text" name="longURL" placeholder="<%= longURL.url%>" style="width: 300px">
        <input type="submit" value="Update">
      </form>
      <td><a href="http://localhost:8080/<%= shortURL %>"><%= longURL.url %></a></td>
      </tr>
      <a href ="http://localhost:8080/urls/"><button>Home</button></a>
      <a href ="http://localhost:8080/home"><button>Profile</button></a>
      </table>
    </body>
  </html>