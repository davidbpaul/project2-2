<!DOCTYPE html>
  <html lang="en">
    <body style = "background-color: lightblue">
      <h1>Short URL</h1>
      <table>

      <tr>
        <td><%= shortURL %></td>
        <td>---></td>
       <form action="/urls/<%= shortURL %>" method="POST" style="margin:150px;">
        <label for="longURL">Update a URL:</label>
        <input id="longURL" type="text" name="longURL" placeholder="<%= longURL %>" style="width: 300px">
        <input type="submit" value="Update">
      </form>
      <td><a href="http://localhost:8080/<%= shortURL %>"><%= longURL %></a></td>
      </tr>
       <a href ="http://localhost:8080/urls/">Return</a>
      </table>
    </body>
  </html>