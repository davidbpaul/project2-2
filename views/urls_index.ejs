<!DOCTYPE html>
  <html lang="en">
    <body style = "background-color: lightblue">
    <% include partials/_header %>
       <a href ="http://localhost:8080/urls/new">Create</a>

     <div class = urls>
     <%for(url in urls){ %>
      <div id = url style = "background-color:lightblue; width: 400px; display:flex;
justify-content: flex-start;">
            <p><%= `${url} --> ` %> <a href="http://localhost:8080/<%= `${url}`  %>"> <%= `${urls[url]}` %></a></p>
            <td></td>
            <a href ="http://localhost:8080/urls/<%= url %>" style = "float:right;padding: 16.5px">Edit</a>
            <form method="POST" action="/urls/<%= url %>/delete" style = "padding: 16.5px"><button>Delete</button></form>
      </div>
             <% } %>
     </div>
    </body>
  </html>